// Default values for map configuration
// Coordinates are in GCJ-02 format (Mars Coordinates) used by AMap
// Format: [longitude, latitude]
export const DEFAULT_MAP_HEIGHT = 400;
export const DEFAULT_MAP_CENTER: [number, number] = [116.4074, 39.9042]; // Beijing (GCJ-02)
export const DEFAULT_MAP_ZOOM = 12;
